# doc-tools Architecture and AI Context

This repository provides a generalized, domain-agnostic document data ingestion pipeline built with Dagster.

## Core Purpose
Process generic documents (PDF, PPTX, DOCX) to extract rich text, metadata, and embedded images. Build a robust knowledge graph in Neo4j representing the semantic layout, and vectorize the content into Weaviate for Retrieval-Augmented Generation (RAG).

## Architecture
- **Dependency Management**: `uv` powers all dependency resolution securely via `pyproject.toml`.
- **Orchestration**: `Dagster` orchestrates the pipeline, using generic dynamic partitions (`doc_id/filename`). Sensors monitor MinIO for raw file drops to automatically trigger ingestion workflows.
- **Config-Driven Domain Mapping**: Uses `dagster.Config` (`IngestionConfig`) and `ConfigurableResource` to dynamically assign graph node schemas (`graph_node_label`, `graph_child_label`) and vector database collections (`vector_collection_name`), keeping the underlying code strictly domain-agnostic.
- **Deployments**: Containerized CI/CD is bound to GitHub Actions using Cloud Native Buildpacks (Paketo). System dependencies for unstructured optical parsing (`libmagic-dev`, `poppler-utils`, `tesseract-ocr`) are natively declared in the `Aptfile`.

## Key Files & Directories
- `doc_tools/assets/ingestion_assets.py`: Layout detection, unstructuring, and intermediate manifest generation. Custom python-pptx extraction is layered here inside.
- `doc_tools/plugins/`: Contains the **Domain-Agnostic Plugin Architecture**. Base structural nodes are passed here where domain logic (Training/MAT) invokes BAML schemas and returns Cypher/SPARQL queries.
- `baml_src/`: LLM prompt definitions mapped to structural schemas. Compiles via `baml-py` into Pydantic models.
- `doc_tools/assets/semantic_assets.py`: Cypher logic to build Neo4j Knowledge Graphs and semantic vectorization into Weaviate. Routes extractions dynamically using a Factory switch pattern against the loaded plugins.
- `doc_tools/definitions.py`: Entrypoint for Dagster binding assets, jobs (`process_documents_job`), sensors (`document_upload_sensor`), and resources.
- `pyproject.toml`: The root configuration of Python parameters.

## AI Instructions
- All code MUST remain domain-agnostic. Do NOT hardcode domain references (e.g., "Course", "Slide").
- Always use the `uv` toolchain for dependency testing.
