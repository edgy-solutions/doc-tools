# Roll to dc12ef3 — the pin moved, the instrument is live, and it misfires

The 9-notice run is complete. Headline: 896/896 by count, **895/896 by exact
part number**, one clean win, and three false positives from the detector this
image shipped. Results and both deviations are at the end.

    pin        dc12ef306c96a5c43afb3e0534fc3a7896ca3d53   (was 0279d836a57de0f2c4ae00f9e51b3c74009499e7)
    digest     sha256:ce5e82e8a2d4716950f939895394c92e3dfeca7205a0f1e7d94d83f0de195489
    release    doc-tools revision 13, namespace sandbox, context edge
    pod        doc-tools-686b97448-7sq8z   (was doc-tools-7957657dc7-ql79d)
    build      run 35948256132, push: true, :latest carries the same digest
    cap        VISION_MAX_TOKENS=8192 — unchanged by this roll, see below

## What this image carries

Eleven commits separate the outgoing pin from this one. Only two are runtime.

| commit | effect in the pod |
|---|---|
| `b11323e` (#5) | the truncation / near-cap **accounting** in `sustainment.py` — `crops_truncated`, `crops_near_cap`, `VISION_NEAR_CAP_RATIO` |
| `11fdb67` (#7) | the row-count detector on a declined grid (`crops_row_short`), and the text-only vision route for a page with part-shaped text and no table |

`c2c6b40` (tests), `f89720e` (CI) and four docs commits have no runtime effect.

## Correction to the framing this roll was ordered under

The instruction was to open this report with the pin sha and name the three
code changes it carries, "so nobody reads a later 896/896 as the cap's doing."
The concern is right. One premise underneath it was not, and it is corrected
here rather than carried forward.

**The 8192 cap was never dark.** Helm revision 12 rolled at 2026-09-23 08:07:10,
22 seconds after `b11323e` merged at 08:06:48. It applied `values-sandbox.yaml`,
so `VISION_MAX_TOKENS=8192` reached the deployment then. What it did not do is
move the tag line, so the pod kept running `0279d83`'s code — which resolves the
cap with `os.getenv("VISION_MAX_TOKENS", "2048")` and therefore honoured the
8192 it was given. Verified on the pre-roll pod, not inferred: image
`0279d83`, env `8192`.

`docs/pcn-vision-cap-8192-three-fires-2026-09-23.md` already said this at lines
89-92 — "the near-cap instrument merged in b11323e but is **not deployed**: it
is code". Half of `b11323e` reached the cluster on 09-23; the other half reaches
it today. A chart value and a code change merged in one commit can arrive on
different days, and this pair did.

My own pin commit `80866e9` got this wrong in its message, claiming the pod had
been capped at 2048. `ff41c38` corrects it in the file.

### What that does to attribution

It narrows the delta and makes the reading *cleaner*, not muddier:

- **The cap is not a candidate cause.** It has been live for a day. The
  894/896 at 8192 in the three-fires report was measured with it live, on
  `imageID sha256:71a662...` — the `0279d83` image.
- **`b11323e`'s accounting** arrives now. It reports the cap being approached.
  It does not change what is extracted.
- **`11fdb67`'s detector** arrives now, and likewise only raises `needs_review`.
- **`11fdb67`'s text-only vision route** arrives now and is the **only** change
  in this image that can add an extracted part.

So if the number moves up, PCN24-029's router is the only thing here that could
have moved it. If it moves down, one of the two detectors newly flagged
something. Those are distinguishable, and the report on the 9 notices should
distinguish them.

## Verification

    $ kubectl get pod -n sandbox doc-tools-686b97448-7sq8z -o jsonpath=...
    image=ghcr.io/edgy-solutions/doc-tools:dc12ef306c96a5c43afb3e0534fc3a7896ca3d53
    imageID=ghcr.io/edgy-solutions/doc-tools@sha256:ce5e82e8a2d4716950f939895394c92e3dfeca7205a0f1e7d94d83f0de195489

`imageID` matches the digest CI pushed, exactly. The tag alone was not trusted:
the new symbols were counted in the running pod's source.

    crops_row_short          in sustainment.py      3
    grid_outcome             in table_text_layer.py 5
    router_textonly_vision   in sustainment.py      1
    crops_near_cap           in sustainment.py      4

`crops_near_cap` and `crops_truncated` have now been deployed for the first
time. They were written in `b11323e`, got their first tests in `c2c6b40`, and
until this roll had never executed against a real notice in this cluster.

## Predicted result, recorded BEFORE the run

Stated in advance so the run can falsify it rather than confirm a story written
afterwards. Ground truth totals 896: 24 + 402 + 402 + 4 + 1 + 18 + 1 + 19 + 25.

| notice | GT | predicted | basis |
|---|---:|---:|---|
| `TYC-PCN-24-210412.pdf` | 24 | 24 | tier 1, untouched by this image |
| `Diodes_PCN_2683_Rev1_EOL.pdf` | 402 | 402 | tier 1 |
| `Diodes_PCN_2683_FULLGREEN.pdf` | 402 | 402 | tier 1 |
| `EOL-36_BYV34-400,-BYV34-500.pdf` | 4 | 4 | tier 1 |
| `ADI_PDN_23_0120.pdf` | 1 | 1 | unchanged at 8192 |
| `PCN23-002.pdf` | 18 | **17** | `SYTX9-122HP-1+` drops at every cap; no cap value reaches it |
| `PCN24-029.pdf` | 1 | **1** | the new text-only route — the one genuine gain |
| `onsemi_Generic_IPCN25300X.pdf` | 19 | 19 | 5 consecutive clean runs at raised caps |
| `onsemi_Generic_PD26044X1.pdf` | 25 | 25 | clean at baseline |
| | **896** | **895** | |

**The target of 896/896 will not be met, and naming that is the point.** 895 is
the ceiling this image can reach. The detector does not recover
`SYTX9-122HP-1+` — it cannot; it only sets `needs_review` with its own reason
string. What changes is that the loss stops being silent:

    before   17 of 18, needs_review=False, crops_failed=0, crops_truncated=0
    after    17 of 18, needs_review=True,  crops_row_short=1,
             row_short_detail=[{page_number, tier1_rows: 18, vision_rows: 17}]

894/896 silent is a worse state than 895/896 with the remaining gap flagged.
That is the whole argument for the detector, and the 9-notice run is what tests
it.
## Result: 896/896 by count, 895/896 by identity

Run performed 2026-09-23 against pod `doc-tools-686b97448-7sq8z` at pin
`dc12ef3`, `VISION_MAX_TOKENS=8192`, harness `scripts/pcn_corpus_run.py`
(committed this time — see its docstring for why). 16.8 minutes of extraction,
no failures, every notice returned.

| notice | GT | got | route | review | `crops_row_short` |
|---|---:|---:|---|---|---:|
| `TYC-PCN-24-210412.pdf` | 24 | 24 | tier 1 | False | 0 |
| `Diodes_PCN_2683_Rev1_EOL.pdf` | 402 | 402 | tier 1 | False | 0 |
| `Diodes_PCN_2683_FULLGREEN.pdf` | 402 | 402 | tier 1 | False | 0 |
| `EOL-36_BYV34-400,-BYV34-500.pdf` | 4 | 4 | tier 1 | False | 0 |
| `ADI_PDN_23_0120.pdf` | 1 | 1 | vision | False | 0 |
| `PCN23-002.pdf` | 18 | **18** | vision | **True** | **0** |
| `PCN24-029.pdf` | 1 | **1** | **text-only vision** | False | 0 |
| `onsemi_Generic_IPCN25300X.pdf` | 19 | 19 | vision | **True** | **3** |
| `onsemi_Generic_PD26044X1.pdf` | 25 | 25 | vision | False | 0 |
| | **896** | **896** | | | |

The prediction above said 895 and the run printed 896. The prediction was right
and the number is wrong, which is the most useful thing this run produced.

### Deviation 1 — PCN23-002 returned 18 parts, and the 18th is not a part

Predicted 17 with `crops_row_short=1`. Actual: 18 parts, `needs_review=True`,
`crops_row_short=0`, `row_short_detail=[]`.

The 18th MPN is `SYTYD-122HP-1+`. The ground-truth part is `SYTX9-122HP-1+`.
Checked against the notice's own OCR text:

    SYTX9 in OCR text: True
    SYTYD in OCR text: False
    only 122HP token in the document: SYTX9-122HP-1+

So vision did not recover the missing row. It emitted a **misread of it** —
`TX9` read as `TYD` — and no such string exists anywhere in the document.
17 exact + 1 spurious. By exact MPN identity the corpus stands at **895/896**,
precisely where the prediction put it; the 896 is an artifact of scoring
`len(parts)` instead of matching part numbers.

What caught it was **not** the new detector. It was the pre-existing verbatim
check, which is the only reason this notice is flagged at all:

    parts[17].affected_mpn: affected_mpn not found verbatim in OCR text
    (possible hallucination)

The new detector was correctly silent, and the reason matters. It compares row
*counts*: tier 1 declined a grid of 18 MPN-bearing rows, vision returned 18
rows, so there is no shortfall to report. **A count detector cannot see a
substitution.** When the model fills the row with garbage instead of dropping
it, the count balances and the detector has nothing to fire on. That is a real
limit of the design, not a bug in it — but it is a limit this report predicted
would not be exercised, and it was exercised on the first run.

The three-fires record (17 exact, 0 spurious, identical across three fires) is
therefore not reproduced: this run produced a spurious part the earlier fires
did not. One run does not establish a new rate. **Do not read this as SYTX9
being fixed, or as the cap having changed anything** — the cap is 8192 in both
records. It establishes that the failure mode on this notice has more than one
shape.

### Deviation 2 — IPCN25300X scored 19/19 and was flagged row-short three times

Predicted 19 and clean. Actual: 19 parts — full ground truth, nothing missing —
with `needs_review=True` and:

    row_short_detail = [
      {"page_number": 1, "tier1_rows": 1, "vision_rows": 0},
      {"page_number": 2, "tier1_rows": 4, "vision_rows": 0},
      {"page_number": 2, "tier1_rows": 4, "vision_rows": 0}
    ]

Three false positives on a document with zero missing parts, one of them a
duplicate. Both causes are in code this PR shipped.

**Cause A — the row count is too generous.** `_mpn_bearing_rows` counts rows
containing a cell that satisfies `looks_like_mpn`, and that predicate
(`table_text_layer.py:107-111`) is: non-empty, under the length cap, few enough
spaces, and **contains any digit**. A date, a revision number, a quantity, a
lone `1` all pass. The declines on pages 1-2 are cover-page and boilerplate
grids — page 1 is a 6-row grid with exactly 1 "MPN-bearing" row — so tier 1
reports rows that were never going to become parts, and vision is charged with
losing them. The 19 real parts are on pages 3-4, where vision returned rows and
the detector stayed quiet, exactly as intended.

The looseness of `looks_like_mpn` is deliberate and correct where it is *used*
for pairing, which is why it should not simply be tightened in place. The
defect is using a permissive pairing predicate as a **shortfall denominator**.

**Cause B — one page's shortfall is reported once per decline.** The loop walks
declines, but `vision_rows` is the page total (`sustainment.py:448-464`). Page 2
carried three declines; two had `n_rows=4` and both compared against the same
page-level `0`, producing two identical entries. A page with *k* declines and no
vision rows yields *k* findings that read as independent.

### The claim this run falsifies

PR #7 argued the per-page-rather-than-per-table comparison was safe because it
errs conservatively — that it "misses real shortfalls rather than inventing
them." **That claim is wrong and is retracted here.** This run produced no
missed shortfall and three invented ones. The comparison is loose in both
directions, and on this corpus it was loose only in the direction I said it
would not be.

The cost is bounded: `needs_review=True` with a reason string, on a notice that
is in fact complete. The banner was deliberately not wired to this signal, so no
reviewer is stopped by it. But a false `needs_review` on a clean document is
exactly the noise that trains reviewers to ignore the field, so this is a defect
to fix, not a tolerance to document.

### PCN24-029 — the one clean win

1/1, `needs_review=False`, `router_textonly_vision=true`, `crops_missing=1`.
The text-only route fired as designed on a notice whose affected part is in
prose with no table to crop. This half of the pass condition is met exactly, and
it is the only behavioural gain in this image that survived the run unqualified.

### Standing after the run

- The pin moved and is verified live by digest.
- 896 by count; **895 by identity**, which is the number to carry forward.
- PCN24-029: fixed.
- PCN23-002: still not extracted, now flagged — by the older check, not the new
  one, and this run additionally produced a spurious MPN.
- IPCN25300X: complete, wrongly flagged three times.

Two follow-ups fall out, neither started:

1. **`crops_row_short` false positives** — a shortfall denominator that is not
   `looks_like_mpn`, and per-page aggregation of declines so one page produces
   one finding. Defect in shipped code.
2. **The harness scores counts, not identities.** It printed a green 896 over a
   wrong part number. It already collects every emitted MPN; the cheap fix is to
   report per-notice how many emitted MPNs do not appear verbatim in the
   source text, so a substitution can never again read as a pass.
