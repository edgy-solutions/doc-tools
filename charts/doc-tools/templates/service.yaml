apiVersion: v1
kind: Service
metadata:
  name: {{ include "doc-tools.fullname" . }}
  labels:
    {{- include "doc-tools.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "doc-tools.selectorLabels" . | nindent 4 }}
