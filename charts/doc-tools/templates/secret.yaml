apiVersion: v1
kind: Secret
metadata:
  name: {{ include "doc-tools.fullname" . }}-secrets
  labels:
    {{- include "doc-tools.labels" . | nindent 4 }}
type: Opaque
stringData:
  {{- range $key, $val := .Values.secrets }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}
