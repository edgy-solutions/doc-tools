apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "doc-tools.fullname" . }}-test-connection"
  labels:
    {{- include "doc-tools.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "doc-tools.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
